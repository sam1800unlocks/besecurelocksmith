---
const { tone = 'neutral', class: cls = '' } = Astro.props;
const colors = tone === 'success'
  ? 'bg-success-bg text-success-2'
  : 'bg-neutral-100 text-ink';
---
<span class={`inline-flex items-center px-3 py-1 rounded-[100px] text-sm font-semibold ${colors} ${cls}`}><slot /></span>
